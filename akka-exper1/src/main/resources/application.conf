akka {

    # from doc example, but doesn't work loggers = ["akka.event.slf4j.Slf4jLogger"]
    actor {
      provider = remote
    }
    remote {
      enabled-transports = ["akka.remote.netty.tcp"]
      netty.tcp {
        hostname = "127.0.0.1"
        port = 10223
      }
    }
}